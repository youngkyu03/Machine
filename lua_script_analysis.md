# CoppeliaSim Lua 코루틴 스크립트 개요

이 문서는 CoppeliaSim에서 진공 그리퍼를 사용하는 Lua 스크립트를 설명하며, 순방향/역기구학을 모두 활용합니다.

## 런타임 훅
- **`sysCall_init`**: `coroutineMain`을 실행하는 코루틴을 생성하여 제어 루프가 단계적으로 동작하도록 합니다.
- **`sysCall_actuation`**: 매 시뮬레이션 스텝마다 코루틴을 재개하고, 코루틴 오류가 발생하면 트레이스백과 함께 전파합니다.

## 페이크 진공 그리퍼 헬퍼
- **`fake_grab(gripperHandle)`**: 그리퍼의 패드 쉐이프(또는 그리퍼 자체)에서 `sim.getContactInfo`로 접촉점을 찾습니다. 다른 쉐이프를 감지하면 다음을 수행합니다.
  - 붙잡힌 객체 이름을 로그로 남깁니다.
  - 객체를 정적으로 전환하고, 동역학을 리셋한 뒤 그리퍼에 부모로 붙여 고정합니다.
- **`fake_release(objHandle)`**: 부모를 해제하고 동적 속성을 복원한 후 객체를 리셋하여 부착을 해제합니다.

## 모션 유틸리티
- **역기구학(`moveToPose_viaIK`)**: 콜백을 사용해 IK 타겟 포즈를 갱신하고, 매 스텝 IK 환경을 적용하면서 `sim.moveToPose`로 이동합니다.
- **순기구학(`FK_move`)**: 전달된 관절 구성에 대해 관절 목표 위치를 직접 설정합니다.

## 픽앤플레이스 시퀀스(`coroutineMain`)
1. **핸들 획득**: 6개 관절, IK 팁/타겟, 모델 베이스, 근접 센서, 픽/테이블 더미, 그리퍼 핸들을 가져옵니다.
2. **IK 설정**: 로봇 체인에 대해 포즈 제약을 가진 IK 환경과 그룹을 생성합니다.
3. **모션 한계**: IK 이동을 위한 최대 속도/가속도/저크와 FK 타겟 및 홈 관절 구성을 정의합니다.
4. **픽 FK 포즈 이동**: FK 타겟을 명령하고 자세가 잡히기를 기다립니다.
5. **센서 대기**: 근접 센서가 물체를 감지할 때까지 대기합니다.
6. **픽 지점 접근**: 픽 더미 포즈를 읽고 상/하 오프셋을 생성한 뒤 IK로 하강합니다.
7. **그립**: `fake_grab`으로 접촉한 객체를 부착하고 IK로 위로 후퇴합니다.
8. **테이블에 배치**: 물체를 잡았다면 테이블 더미 포즈와 상/하 오프셋으로 이동해 배치 위치에 접근합니다(릴리스는 주석 처리됨).
9. **홈 복귀**: 홈 FK 포즈를 명령하고 대기한 후 시뮬레이션을 종료합니다.

## 동작 메모
- 그립 시퀀스는 접촉 감지에 의존하므로 쉐이프를 찾지 못하면 경고를 남기고 배치를 건너뜁니다.
- 릴리스는 현재 주석 처리되어 있어 배치 후에도 객체가 부착된 상태로 남습니다.
- 모든 대기는 `sim.wait`를 사용하므로 코루틴이 시뮬레이터 스케줄러에 양보해 부드럽게 진행됩니다.

## 중간에 멈출 수 있는 대표 원인
- **근접 센서 대기 구간(무한 대기)**: `while sim.readProximitySensor(sensor) <= 0` 루프에서 감지가 없으면 다음 단계로 넘어가지 않아 정지한 것처럼 보일 수 있습니다. 센서 위치·감도·장면 배치를 점검하세요.
- **그랩 실패 후 반환**: `fake_grab`은 탐지된 객체가 없으면 `nil`을 반환하고 이후 배치 단계가 건너뛰어집니다. 이 경우 로봇은 픽업 후 바로 홈으로 복귀하며 작업이 갑자기 끝난 것처럼 느껴집니다. 그리퍼 주변의 콜리전 레이어와 물체 유형(쉐이프인지)을 확인하세요.
- **모든 스텝 완료 후 시뮬레이션 종료**: 시퀀스 끝에서 `sim.stopSimulation()`을 호출하므로 홈 복귀 직후 시뮬레이션이 즉시 멈춥니다. 디버깅 시에는 종료 호출을 임시로 주석 처리하여 이후 상태를 관찰할 수 있습니다.
